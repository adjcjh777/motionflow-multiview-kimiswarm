#!/usr/bin/env bash
# Launcher template for the iter20 ablation matrix.
# Edit the per-row flags and GPU assignments before running.
set -euo pipefail

echo "Ablation matrix launcher -- see docs/ablation_study_design.md for details."
echo "Run each row with CUDA_VISIBLE_DEVICES=<gpu> and a unique tmux session."
exit 0
