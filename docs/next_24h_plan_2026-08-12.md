# Next 24-Hour Plan — MotionFlow-MultiView

**Window:** 2026-08-12 12:45 UTC → 2026-08-13 12:45 UTC  
**Scope:** Protect the v85 random-view-dropout run to completion, capture its variable-view robustness, and unblock SOTA baseline prep.  
**Prepared:** 2026-08-12 12:47 UTC  

---

## 1. Current snapshot (as of 2026-08-12 ~12:47 UTC)

| Item | Status |
|---|---|
| **v85 random view dropout training** | **RUNNING** on A800 **GPU 7** (PID `2058223`, elapsed 01:13). Epoch 2 done: train_loss 14.91, **val_MPJPE 36.48 mm**; Epoch 3 in progress, loss falling. Script: `scripts/run_v85_random_view_dropout_medium_a800.sh` (epochs=20, train_samples=4096, batch=16, early stopping expected). |
| **v85 no-fallback split-k eval** | **RUNNING** on A800 **GPU 6** (PID `2148511`, elapsed ~04:00). Split-k script started k=2 at 12:45 UTC, but `outputs/variable_view_v85_random_view_dropout_medium_a800_k2.log` is still **0 bytes** — likely hung in DataLoader/initialization. |
| **Post-training eval suite monitor** | **RUNNING** (PID `2072251`, elapsed 00:56:29). Waits for v85 training, then launches test-set eval, no-fallback variable-view eval, and DLT-fallback eval on first free GPU. Log: `outputs/sota_baselines/monitor_v85_then_run_evals.log`. |
| **VoxelPose auto-launch monitor** | **RUNNING** (PID `2146696`, elapsed 00:07:11). Waits for v85 eval suite, then launches VoxelPose. |
| **Disk on A800 `/mnt/nvme0n1p1`** | **99% full**, ~58 GB free. |
| **Local WSL RTX 4090** | Idle (~1.8 GB used); no active local training. A detached local SSH process (PID `1589`) is forwarding the A800 split-k eval session. |

**GPU policy:** only GPUs 6/7 may be used; 0–5 are reserved.  
**Remote repo:** `/mnt/nvme0n1p1/zhangzy/motionflow-multiview-kimiswarm-iter20`  

---

## 2. Next 24h goals

1. **Land v85 training safely.** Let it run to completion/early-stop; do not kill unless it crashes or overfits. Capture final val/test MPJPE and checkpoint path.
2. **Resolve the hung v85 split-k no-fallback eval.** If k=2 log is still empty after a reasonable wait, kill it so GPU 6 is free and the post-training monitor can launch a fresh, consistent eval using the final checkpoint.
3. **Run the v85 post-training eval suite.** Test-set eval, no-fallback variable-view eval (k=2/3/4), and DLT-fallback eval (k=2/3/4) on the first free GPU.
4. **Compare v85 sparse-view numbers to the v25 stability DLT-fallback baseline** (S9: 58.18/33.32/116.98 mm; S11: 49.35/25.28/110.58 mm for k=2/3/4).
5. **Free disk space** with a safe dry-run of `scripts/cleanup_a800_safe.sh` before launching any new large job.
6. **Prepare SOTA comparison configs** (VoxelPose / MVPose / DLT) so they can launch once GPU 6/7 is free after the v85 suite.
7. **Update docs:** `docs/results_true_gt_h36m.md` and `docs/paper_draft_icra_cvpr_2027.md` with v85 numbers and the next backlog.

---

## 3. Owner assignments

| Owner | Primary role | Secondary / CPU work |
|---|---|---|
| **Current agent / qwen3.8max** | Monitor v85 training/eval; kill/restart hung eval; capture results. | Disk cleanup dry-run; update `docs/results_true_gt_h36m.md` and paper draft. |
| **agent-51** (if available) | Support SOTA baseline config prep; run cleanup if needed. | Validate VoxelPose/MVPose configs on A800. |
| **agent-67** (if available) | AIST++ cross-domain prep; MPI/Shelf-Campus doc updates. | Draft SOTA comparison tables. |

---

## 4. Hour-by-hour timeline

### Phase 0 — Stabilise active runs (12:45–15:00 UTC)

| Time | Owner | Task | Resource | Deliverable |
|---|---|---|---|---|
| 12:45–13:15 | Current | Watch v85 k=2 no-fallback eval. If log remains 0 bytes for >30 min, kill the split-k session. | A800 GPU 6 | Decision: keep running or free GPU. |
| 12:45–14:00 | Current | Monitor v85 training tail; confirm Epoch 3 loss continues falling. | A800 GPU 7 | Log snippet confirming healthy training. |
| 14:00–15:00 | Current | If k=2 eval is hung, document and kill it; verify GPU 6 memory drops. | A800 GPU 6 | GPU 6 idle or fresh eval launched by monitor. |

**Decision point:** The split-k no-fallback eval is a manual run on a *stale* epoch-2 checkpoint. The post-training monitor will run a fresh no-fallback eval on the *final* checkpoint once training finishes. If the split-k eval is hung, it is safe to kill it.

### Phase 1 — v85 training completion (15:00–23:00 UTC, estimated)

| Time | Owner | Task | Resource | Deliverable |
|---|---|---|---|---|
| 15:00–23:00 | Current | Leave v85 training untouched; tail log every 1–2 h for crashes/overfit. | A800 GPU 7 | Training completes or early-stops; final `.pth` + `.log`. |
| 15:00–18:00 (parallel) | Current/agent-51 | Run `scripts/cleanup_a800_safe.sh --dry-run`; identify removable old checkpoints (v83/v84, failed runs). | CPU/A800 | Dry-run report. |
| 18:00–23:00 (parallel) | agent-51 / agent-67 | Prepare SOTA configs under `scripts/sota_baselines/` and `configs/`. | CPU | Configs ready to launch. |

### Phase 2 — Post-training eval suite (training finish → +6 h)

| Time | Owner | Task | Resource | Deliverable |
|---|---|---|---|---|
| T+0 | Post-training monitor (auto) | Launches v85 test-set eval on first free GPU. | A800 GPU 6 or 7 | `outputs/eval_v85_random_view_dropout_medium_a800_test.json` |
| T+1–3 h | Monitor (auto) | Launches no-fallback variable-view eval (k=2/3/4). | A800 GPU 6 or 7 | `outputs/variable_view_v85_random_view_dropout_medium_a800*.csv/json` |
| T+3–6 h | Monitor (auto) | Launches DLT-fallback variable-view eval (k=2/3/4). | A800 GPU 6 or 7 | DLT-fallback JSON/CSV. |
| In parallel | Current | Validate outputs against v25 stability baseline; update leaderboard. | CPU | Updated `docs/results_true_gt_h36m.md`. |

### Phase 3 — SOTA baselines / cleanup (08:00–12:00 UTC next day)

| Time | Owner | Task | Resource | Deliverable |
|---|---|---|---|---|
| 08:00–10:00 | Current / agent-51 | If GPU 6/7 free, run cleanup (if safe) and launch first SOTA baseline smoke (DLT or VoxelPose). | A800 GPU 6/7 | Smoke log + JSON. |
| 10:00–12:00 | Current | Update `docs/paper_draft_icra_cvpr_2027.md` with v85 results and SOTA smoke status; write handoff. | CPU | Updated paper draft + handoff doc. |

---

## 5. Blockers and contingencies

| Blocker | Impact | Owner | Contingency |
|---|---|---|---|
| **v85 split-k eval hung on k=2** | Blocks GPU 6 until fixed. | Current | Kill it; post-training monitor will re-run after training. |
| **A800 disk 99% full** | v85 training/eval may fail writing checkpoints. | Current / agent-51 | Run `cleanup_a800_safe.sh --dry-run` first; delete only obviously stale outputs (failed v83/v84, old `.failed_*` logs). |
| **v85 training overfits or crashes** | No final checkpoint for eval suite. | Current | If val_MPJPE rises >50 mm for two epochs, consider early manual stop; use best saved checkpoint. |
| **v85 k<4 still catastrophic** | Sparse-view robustness not achieved. | Current / agent-51 | Compare to v25 DLT-fallback; consider stronger count-conditioning or separate sparse-view head in follow-up. |
| **SOTA configs need GPU time** | Cannot launch until v85 suite frees GPU. | agent-51 / agent-67 | Prepare configs on CPU; queue immediately after monitor finishes. |

---

## 6. Success criteria for the 24-hour window

- [ ] v85 training finishes or early-stops with a saved best checkpoint.
- [ ] v85 test-set eval completed and JSON available.
- [ ] v85 variable-view no-fallback and DLT-fallback evals completed (or in progress with final checkpoint).
- [ ] `docs/results_true_gt_h36m.md` updated with v85 numbers.
- [ ] Disk cleanup dry-run completed and safe deletions documented.
- [ ] SOTA baseline configs ready to launch.
- [ ] `docs/paper_draft_icra_cvpr_2027.md` results section updated with latest true-GT/v85 numbers.

---

## 7. Quick verification commands

```bash
# v85 training tail
ssh -o ServerAliveInterval=60 a800-D \
  "tail -n 20 /mnt/nvme0n1p1/zhangzy/motionflow-multiview-kimiswarm-iter20/outputs/ablations/v85_random_view_dropout_medium_a800.log"

# v85 split-k no-fallback eval status
ssh -o ServerAliveInterval=60 a800-D \
  "ls -lh /mnt/nvme0n1p1/zhangzy/motionflow-multiview-kimiswarm-iter20/outputs/variable_view_v85_*"

# Post-training eval monitor tail
ssh -o ServerAliveInterval=60 a800-D \
  "tail -n 20 /mnt/nvme0n1p1/zhangzy/motionflow-multiview-kimiswarm-iter20/outputs/sota_baselines/monitor_v85_then_run_evals.log"

# GPU status
ssh -o ServerAliveInterval=60 a800-D \
  "nvidia-smi --query-gpu=index,utilization.gpu,memory.used --format=csv,noheader"

# Disk status
ssh -o ServerAliveInterval=60 a800-D \
  "df -h /mnt/nvme0n1p1"
```

---

## 8. Notes for the next agent

- **Do not launch new training/eval jobs unless this plan explicitly says so.** The post-training monitor is already queued.
- If the split-k no-fallback eval is still hung when you take over, kill PID `2148511` (and its parent `2148510`) on A800, then verify GPU 6 memory drops.
- v85 training is the only high-priority GPU job. Let it finish.
- Disk cleanup is the second priority; do it before any new large job.
- SOTA baselines are third priority and depend on GPU 6/7 being free.
